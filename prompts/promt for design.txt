Redesign the provided HTML/CSS page to match the TalentMatch Glassmorphism Design System while maintaining full functional compatibility with our React feature architecture.

### Design System Specification

1. COLOR PALETTE & TOKENS:
   - Background: Linear gradient from white to soft icy blue-tint `linear-gradient(135deg, #ffffff 0%, #f8f9ff 100%)`
   - Glass Surface (`.glass-card` / `--panel`): Translucent white `rgba(255, 255, 255, 0.7)` with `backdrop-filter: blur(15px)` and border `1px solid rgba(255, 45, 45, 0.15)`
   - Primary Text (`--fg`): Dark navy `#1a1a2e`
   - Muted Text (`--muted`): Slate `#6b6964`
   - Primary Accent (`--accent`): Vibrant Red `#ff2d2d`
   - Accent Glow (`--accent-glow`): `rgba(255, 45, 45, 0.15)`

2. TYPOGRAPHY & ELEVATION:
   - Font: Apple/System sans-serif (`-apple-system, BlinkMacSystemFont, 'SF Pro Text', system-ui, sans-serif`)
   - Interactive Elements: Standard smooth hover effects (`transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1)`)
   - Card Hover States: Border transitions to `--accent` with elevation shadow `box-shadow: 0 12px 32px var(--accent-glow)` and minor vertical lift (`transform: translateY(-4px)`)

3. LAYOUT & STRUCTURE:
   - Sidebar: Sticky left-side navigation with a translucent glass background, bold logo in `--accent`, active navigation tab with red gradient background (`linear-gradient(135deg, #ff2d2d 0%, #ff5555 100%)`) and glow box-shadow
   - Header: Top sticky header with `backdrop-filter: blur(10px)` and bottom border `1px solid rgba(255, 45, 45, 0.15)`
   - Tags/Badges: Light red transparent background `rgba(255, 45, 45, 0.08)`, subtle border, and red text

4. ACTION BUTTONS:
   - Primary Button (`.btn-red`): Red gradient `linear-gradient(135deg, #ff2d2d 0%, #ff5555 100%)`, white text, soft glow shadow
   - Secondary Button (`.btn-secondary`): Glass panel background, subtle border, dark text, turns white with red border on hover

### Task:
Apply this theme directly to the HTML structure below. Output the complete redesigned HTML with embedded CSS so it matches the exact styling and visual aesthetic of the Candidate Pipeline page.

---
RAW HTML TO STYLIZE:
